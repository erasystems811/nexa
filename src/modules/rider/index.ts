/**
 * Rider — Rider App's server logic. PRD Section 15.
 *
 * Owns: riders, rider_documents, rider_reliability, rider_wallets,
 * rider_assignments.
 *
 * Assignment reads riders.vehicle_type: a bulk load never goes to a bike.
 * A rider is never attached to a service booking — the DB refuses (0007).
 * Phase 1: empty.
 */
export {};
