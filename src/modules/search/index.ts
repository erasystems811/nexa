/**
 * Search — discovery over listings and providers. PRD Section 07.
 *
 * Owns: no tables. Reads the marketplace model.
 * Only available listings from approved providers are ever returned; that is
 * an RLS guarantee, not a filter this module has to remember to apply.
 * Phase 1: empty.
 */
export {};
