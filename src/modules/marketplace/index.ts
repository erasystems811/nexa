/**
 * Marketplace — the customer surface. PRD Sections 07, 14.
 *
 * Owns: the public read model over listings, providers, categories, favourites.
 * Plan My Event is a curation layer over Bookings, not a second engine — it
 * stays behind the `plan_my_event` flag until Phase 2.
 * Phase 1: empty.
 */
export {};
