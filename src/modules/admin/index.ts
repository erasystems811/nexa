/**
 * Admin — the Admin Console's server logic. PRD Section 12.
 *
 * Owns: verification queues, listing approval, order monitoring, dispute
 * resolution, manual rider (re)assignment, audit_log.
 *
 * May call every other module. Nothing calls it.
 * Phase 1: settings and feature flags only; those live in `modules/settings`.
 */
export {};
