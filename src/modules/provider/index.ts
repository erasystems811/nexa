/**
 * Provider — Business Studio's server logic. PRD Sections 05, 13.
 *
 * Owns: providers, provider_categories, provider_documents,
 * provider_agreements, provider_strikes, provider_reliability, provider_wallets.
 *
 * A provider only ever touches their own business; enforced in RLS, not here.
 * Phase 1: empty.
 */
export {};
