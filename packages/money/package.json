{
  "name": "@nexa/money",
  "version": "0.0.0",
  "private": true,
  "description": "Kobo-integer money math and NGN formatting — used by api-server (escrow calculations) and every frontend (price display). Zero dependencies.",
  "main": "./src/money.ts",
  "types": "./src/money.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
