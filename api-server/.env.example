PORT=4000

# Same Supabase project every app in the monorepo uses.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated frontend origins allowed to call this server.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
