/**
 * Users — profile reads and writes that are not authentication.
 *
 * Owns: profiles.
 * Phase 1: empty. The role a profile carries is set by the provider
 * approval triggers or by an admin, never here (see 0011_rls.sql).
 */
export {};
