{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules && npm cache clean --force && npm install && npm run build --workspace apps/studio"
  },
  "deploy": {
    "startCommand": "npm run start --workspace apps/studio",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
