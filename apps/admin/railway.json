{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p node_modules && find node_modules -mindepth 1 -maxdepth 1 ! -name .cache -exec rm -rf {} + && npm cache clean --force && npm install && npm run build --workspace apps/admin"
  },
  "deploy": {
    "startCommand": "npm run start --workspace apps/admin",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
